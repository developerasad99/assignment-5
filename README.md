1. What is the difference between var, let, and const?
   Ans :
   var is a not block scoped ,it is univarsal.
   let is block scoped variable, it can re assigned.
   const is a block scoped variable but it can't re assigned.

2. What is the spread operator (...)?
   Ans :
   this is a best operator for array and object, it show all item individually .

3. What is the difference between map(), filter(), and forEach()?
   Ans:
   map() retern a new array to the previous array
   filter() retern a new array with condition filtering
   forEach create a new array but not retern anything imedietly.

4. What is an arrow function?
   Ans :
   arrow function retern automaticly in single line but it have save behaviour like regular function when it will be multiple line rendering.

5. What are template literals?
   Ans:
   this is a most usefull system in js, it will be provide right to write js all variable,function, object etc in string , it not a reguler string it start with `` it, and write js in string like ${} this block scoped.
